@{
  Layout = "_Layout";
}

@using MTGDeck.Models;
@model MTGDeck.Models.Card

  <div class="row row-cols-2 row-cols-md-2 g-4">
    <ul>
        <li>@Html.ActionLink($"{Model.Name}", "Details", new { name = Model.Name })</li>
        <li>@Model.Type_Line</li>
        <li>@Model.Set_Name</li>
        <li>@Model.Mana_Cost</li>
    </ul>
  </div>